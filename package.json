{
  "name": "dentpilot-university-production-core",
  "private": true,
  "version": "1.0.0-phase1",
  "description": "Production-grade modular monolith foundation for DentPilot University.",
  "engines": { "node": ">=22.0.0", "npm": ">=10.0.0" },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm run build --workspace @dentpilot/contracts && npm run build --workspace @dentpilot/domain && npm run build --workspace @dentpilot/config && npm run build --workspace @dentpilot/api && npm run build --workspace @dentpilot/web",
    "typecheck": "npm run build --workspace @dentpilot/contracts && npm run build --workspace @dentpilot/domain && npm run build --workspace @dentpilot/config && npm run typecheck --workspace @dentpilot/api && npm run typecheck --workspace @dentpilot/web",
    "lint": "npm run build:packages && npm run lint --workspace @dentpilot/api && npm run lint --workspace @dentpilot/web",
    "build:packages": "npm run build --workspace @dentpilot/contracts && npm run build --workspace @dentpilot/domain && npm run build --workspace @dentpilot/config",
    "test": "npm run build:packages && vitest run --config vitest.config.ts",
    "test:unit": "npm run build:packages && vitest run --config vitest.config.ts tests/unit",
    "test:integration": "npm run build:packages && vitest run --config vitest.config.ts tests/integration",
    "test:migration": "npm run build:packages && vitest run --config vitest.config.ts tests/integration/clean-migration.test.ts",
    "db:migrate": "node --import tsx database/migrations/run.ts",
    "db:seed": "node --import tsx database/seeds/seed.ts",
    "api:dev": "tsx watch apps/api/src/server.ts",
    "web:dev": "npm run dev --workspace @dentpilot/web",
    "infra:up": "docker compose -f infra/docker-compose.yml up -d",
    "infra:down": "docker compose -f infra/docker-compose.yml down -v",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run build",
    "deps:audit": "npm audit --omit=dev"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/pg": "^8.11.11",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  }
}
